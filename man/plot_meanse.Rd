% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-line.R
\name{plot_meanse}
\alias{plot_meanse}
\title{Plot mean with error bars (line chart)}
\usage{
plot_meanse(
  data,
  group_var = NULL,
  target_var,
  time_var,
  legend_title = "组别",
  xlab = "随访时间",
  ylab = "值",
  colors = c("#B5D1E8", "#EB938F", "#A3D9A5", "#F2C68F", "#D1B3DF"),
  single_color = "#B5D1E8",
  test_method = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{group_var}{Character string or NULL. Name of the grouping variable.
If \code{NULL}, a single-group plot is generated. Default is \code{NULL}.}

\item{target_var}{Character string. Name of the continuous outcome variable.}

\item{time_var}{Character string. Name of the time variable. Must contain parseable
numeric values for proper x-axis ordering.}

\item{legend_title}{Character string. Title for the legend. Default is \code{"组别"}.}

\item{xlab}{Character string. X-axis label. Default is \code{"随访时间"}.}

\item{ylab}{Character string. Y-axis label. Default is \code{"值"}.}

\item{colors}{Character vector. Colors for multi-group lines. Must have length >= number of
groups. Default is \code{c("#B5D1E8", "#EB938F", "#A3D9A5", "#F2C68F", "#D1B3DF")}.}

\item{single_color}{Character string. Color for single-group line. Default is \code{"#B5D1E8"}.}

\item{test_method}{Character string or \code{NULL}. For a plot with exactly two groups,
use \code{"t"} for Welch's two-sample t-test or \code{"wilcox"} for the Wilcoxon rank-sum
test at each time point. Significant results are marked with stars. Default is \code{NULL}.}
}
\value{
Invisibly returns a list with:
\itemize{
\item \code{summary_data}: data frame of computed means, SDs, SEs
\item \code{test_data}: time-specific test results, or \code{NULL} when no test is requested
\item \code{plot}: the ggplot object
}
}
\description{
Creates a publication-quality line chart showing group means with 95\% confidence
interval error bars. Supports both single-group and multi-group comparisons.
}
\details{
Time variable must contain extractable numeric values via \code{readr::parse_number()}.
For multi-group plots, ensure \code{colors} has enough values for all groups.
Significance labels use \code{*} for p < 0.05, \verb{**} for p < 0.01, \verb{***} for
p < 0.001, and \verb{****} for p < 0.0001. Tests require exactly two groups.
}
\examples{
\dontrun{
plot_meanse(
  data = subset(ChickWeight, Diet \%in\% c(1, 2)),
  target_var = "weight",
  time_var = "Time",
  group_var = "Diet",
  test_method = "wilcox"
)
}

}
