% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-survival.R
\name{plot_forest}
\alias{plot_forest}
\title{Plot forest plot with broken axis}
\usage{
plot_forest(
  data,
  ci_column = "OR (95\% CI)",
  zero = NULL,
  x_ticks = c(0, 1, 2, 4, 6),
  output_name = "TCM_Forestplot.png",
  width = 7.5,
  xlab = "Odds Ratio (95\% CI)",
  graph_ratio = NULL,
  boxsize = 0.25,
  col_box = "#1f77b4"
)
}
\arguments{
\item{data}{A data frame containing text data to display on the left side of the plot.}

\item{ci_column}{Character or integer. Column name or index containing CI strings like
\code{"1.25 (1.05-1.55)"}. Default is \code{"OR (95\% CI)"}.}

\item{zero}{Numeric or numeric vector. The vertical reference line position. If \code{NULL}
(default), it is auto-detected from the column name (1 for OR/HR/RR, 0 for Beta/MD).}

\item{x_ticks}{Numeric vector. X-axis tick positions. Default is \code{c(0, 1, 2, 4, 6)}.}

\item{output_name}{Character string. Output PNG filename. Default is \code{"TCM_Forestplot.png"}.}

\item{width}{Numeric. Plot width in inches. Default is \code{7.5}.}

\item{xlab}{Character string. X-axis label. Default is \code{"Odds Ratio (95\% CI)"}.}

\item{graph_ratio}{Numeric or \code{NULL}. Proportion of the available plot width used by
the forest graph. Must be strictly between 0 and 1. If \code{NULL} (default), the width
is determined automatically by \code{forestplot}.}

\item{boxsize}{Numeric. Size of the forest plot squares. Default is \code{0.25}.}

\item{col_box}{Character string. Color for boxes and lines. Default is \code{"#1f77b4"}.}
}
\value{
Invisibly returns \code{NULL}. The plot is saved to \code{output_name}.
}
\description{
Creates a publication-quality forest plot based on the \code{forestplot} package, designed
for medical statistical results (OR/HR/RR/Beta). Automatically extracts confidence
interval values from text strings and implements a "broken axis" effect between the
table and the forest plot.
}
\details{
\strong{Smart CI extraction}: The function uses regex to parse point estimates and CI bounds
from strings like \code{"1.25 (1.05-1.55)"} or \code{"1.00 (Ref)"}.

\strong{Smart zero line}: When \code{zero = NULL}, the function auto-detects from the column name
whether to use 1 (for OR/HR/RR/Ratio) or 0 (for Beta/MD).
}
\note{
To preserve leading spaces in variable names, use \code{read_excel(..., trim_ws = FALSE)}.
}
