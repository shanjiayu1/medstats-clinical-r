% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-diagnostic.R
\name{plot_roc}
\alias{plot_roc}
\title{Plot ROC curve with AUC and optimal cutoff}
\usage{
plot_roc(
  data,
  true_var,
  pred_var,
  title = "ROC curve",
  line_color = "#2E86AB",
  show_print = TRUE,
  xlab = "1 - Specificity (False Positive Rate)",
  ylab = "Sensitivity (True Positive Rate)"
)
}
\arguments{
\item{data}{A data frame.}

\item{true_var}{Character string. Name of the true binary outcome variable (0/1).}

\item{pred_var}{Character string. Name of the predicted probability variable.}

\item{title}{Character string. Plot title. Default is \code{"ROC curve"}.}

\item{line_color}{Character string. ROC curve color. Default is \code{"#2E86AB"}.}

\item{show_print}{Logical. Whether to print statistics to console. Default is \code{TRUE}.}

\item{xlab}{Character string. X-axis label. Default is
\code{"1 - Specificity (False Positive Rate)"}.}

\item{ylab}{Character string. Y-axis label. Default is
\code{"Sensitivity (True Positive Rate)"}.}
}
\value{
Displays the plot and invisibly returns a list with:
\itemize{
\item \code{roc_obj}: the pROC roc object
\item \code{auc}: AUC value
\item \code{ci}: 95\% CI (lower, upper)
\item \code{best_threshold}: data frame with optimal cutoff, sensitivity, specificity
\item \code{hl_test}: Hosmer-Lemeshow test result
\item \code{plot}: the ggplot object
}
}
\description{
Generates a ROC curve with AUC, 95\% CI, optimal cutoff (Youden Index),
and optional Hosmer-Lemeshow goodness-of-fit test.
}
\examples{
\dontrun{
model <- glm(am ~ mpg + hp + wt, data = mtcars, family = binomial)
mtcars$pred <- predict(model, type = "response")
plot_roc(mtcars, true_var = "am", pred_var = "pred")
}

}
