% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-diagnostic.R
\name{plot_rcs}
\alias{plot_rcs}
\title{Plot restricted cubic spline (RCS) curve}
\usage{
plot_rcs(
  data,
  exposure,
  outcome,
  covars = NULL,
  nk = 4,
  model_type = c("logistic", "linear", "cox"),
  xlab = NULL,
  ylab = NULL,
  xlim = NULL,
  ylim = NULL,
  trim_quantile = c(0.1, 0.9),
  line_color = "#1F4E79",
  ribbon_color = "#6FA8DC",
  ref_color = "#7F7F7F",
  line_size = 1.2,
  ribbon_alpha = 0.22,
  base_size = 18
)
}
\arguments{
\item{data}{A data frame.}

\item{exposure}{Character string. Name of the exposure variable.}

\item{outcome}{Character string. Name of the outcome variable. For Cox models, use
\code{"Surv(time, status)"} syntax.}

\item{covars}{Character vector or NULL. Names of covariates to adjust for. Default is \code{NULL}.}

\item{nk}{Integer. Number of knots for the RCS. Default is \code{4}.}

\item{model_type}{Character. One of \code{"logistic"}, \code{"linear"}, or \code{"cox"}. Default is \code{"logistic"}.}

\item{xlab}{Character string or NULL. X-axis label. If \code{NULL}, uses \code{exposure}. Default is \code{NULL}.}

\item{ylab}{Character string or NULL. Y-axis label. If \code{NULL}, auto-generated based on
\code{model_type}. Default is \code{NULL}.}

\item{xlim}{Numeric vector or NULL. X-axis limits. Default is \code{NULL}.}

\item{ylim}{Numeric vector or NULL. Y-axis limits. Default is \code{NULL} (auto-computed).}

\item{trim_quantile}{Numeric vector. Quantiles for trimming extreme exposure values.
Default is \code{c(0.1, 0.9)}.}

\item{line_color}{Character string. Color of the main line. Default is \code{"#1F4E79"}.}

\item{ribbon_color}{Character string. Color of the confidence ribbon. Default is \code{"#6FA8DC"}.}

\item{ref_color}{Character string. Color of the reference line. Default is \code{"#7F7F7F"}.}

\item{line_size}{Numeric. Line width. Default is \code{1.2}.}

\item{ribbon_alpha}{Numeric. Ribbon transparency (0-1). Default is \code{0.22}.}

\item{base_size}{Numeric. Base font size for the theme. Default is \code{18}.}
}
\value{
Displays the plot and invisibly returns a list with:
\itemize{
\item \code{fit}: the fitted model object
\item \code{anova}: ANOVA results
\item \code{pred}: prediction object
\item \code{plot}: the ggplot object
\item \code{cross_x}: x-values where the curve crosses the reference line
}
}
\description{
Generates a restricted cubic spline curve for logistic, linear, or Cox models,
displaying the dose-response relationship between an exposure and outcome.
Automatically computes ANOVA p-values and nonlinearity p-values.
}
\details{
\itemize{
\item Categorical covariates must be pre-converted to factors.
\item For logistic models, the outcome must be numeric 0/1.
\item The function uses \code{rms::lrm()}, \code{rms::ols()}, or \code{rms::cph()} depending on \code{model_type}.
}
}
\examples{
\dontrun{
# Logistic model
plot_rcs(mtcars, exposure = "hp", outcome = "am", model_type = "logistic")

# Linear model
plot_rcs(mtcars, exposure = "wt", outcome = "mpg", model_type = "linear")

# Cox model
plot_rcs(lung, exposure = "age", outcome = "Surv(time, status)", model_type = "cox")
}

}
